mutation AddCategory(
	$category: String!
	$iconId: String!
	$userId: UUID
) {
	insertIntoCategoryCollection(
		objects: [
			{ name: $category, icon_id: $iconId, user_id: $userId }
		]
	) {
		affectedCount
		records {
			id
			name
			icon_id
			user_id
		}
	}
}
