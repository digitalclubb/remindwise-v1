query getReminder($slug: BigInt) @load {
	reminders: remindersCollection(filter: { id: { eq: $slug } }) {
		list: edges {
			reminder: node {
				name
				type
				company
				cost
				date
				frequency
				autoRenewal
				notes
				category {
					id
					name
				}
			}
		}
	}
}
