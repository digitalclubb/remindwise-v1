query GetSettings @cache(policy: NetworkOnly) {
	settings: settingsCollection {
		list: edges {
			setting: node {
				id
				first_name
				last_name
				email
				notice_period
				interval
				currency
			}
		}
	}
}
