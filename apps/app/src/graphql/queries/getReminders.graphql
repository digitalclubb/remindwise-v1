query getReminders($categoryId: BigInt) {
	reminders: remindersCollection(filter: { categoryId: { eq: $categoryId } }) {
		list: edges {
			reminder: node {
				id
				name
				company
				cost
				category {
					iconId
					name
				}
			}
		}
	}
}
