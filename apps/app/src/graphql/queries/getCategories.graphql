query GetCategories @cache(policy: NetworkOnly) {
	categories: categoryCollection {
		list: edges {
			category: node {
				id
				name
				icon_id
				reminders: reminderCollection {
					totalCount
					list: edges {
						reminder: node {
							name
							cost
							type
						}
					}
				}
			}
		}
	}
}
