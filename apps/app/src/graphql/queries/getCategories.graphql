query GetCategories @load {
	categories: categoriesCollection {
		list: edges {
			category: node {
				id
				name
				iconId
				reminders: remindersCollection {
					totalCount
					list: edges {
						reminder: node {
							name
							cost
							type
						}
					}
				}
			}
		}
	}
}
