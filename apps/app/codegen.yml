schema: ${PUBLIC_SUPABASE_GRAPHQL_URL}
documents:
  - '**/*.{graphql,gql}'
generates:
  src/graphql/types.ts:
    plugins:
      - typescript
      - typescript-operations
      - typed-document-node
    config:
      useTypeImports: true
